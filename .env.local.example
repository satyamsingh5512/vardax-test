# VARDAx Configuration
VARDAX_API_URL=http://localhost:8000
VARDAX_MODE=monitor  # 'monitor' (logs only) or 'protect' (blocks threats)
VARDAX_FAIL_OPEN=true  # true = allow if VARDAx is down, false = block if down
